custom:
- terraform:
    description: "Custom Action"
    arguments:
      - "custom"
    flags:
      logLevel: TRACE
      backendConfig:
        - "key=my.tfstate"
      vars:
        - "myvar=foo"
    outputs:
      - name: myoutput
